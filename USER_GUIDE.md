# Shanzhai Game Factory 使用手册

## 三步流程

1. `01 TEMPLATE`：选择 `Minesweeper`、`Tetris` 或 `Pac-Man`。
2. `02 ASSETS`：选择一个或多个活动包装元素，再点击 `Build & Run`。
3. `03 RUN`：直接游玩所选小游戏。Assets 会改变游戏配色、棋盘、方块、角色和装饰，但不会改变游戏规则。

返回上一步或刷新页面时，模板与 Assets 会通过浏览器 `localStorage` 保留。

## Minesweeper

- 鼠标左键：翻开格子。
- 鼠标右键：插旗或取消旗帜。
- 第一格不会触雷，零数字区域会自动展开。
- 翻开全部安全格获胜，点到地雷失败。
- 顶部显示剩余雷数和已翻开的安全格数量。

## Tetris

- `←` / `→`：移动方块。
- `↓`：加速下降。
- `↑`：旋转方块。
- `Space`：快速落到底部。
- 也可以点击游戏下方的方向、旋转和 `Drop` 按钮。
- 完整横行会被消除并增加分数；方块堆到顶部时失败。

## Pac-Man

- 方向键或 `WASD`：在迷宫中移动。
- 玩家无法穿过墙体。
- 收集信号豆增加分数；移动的敌人会使玩家失去生命。
- 收集全部信号豆获胜，生命值降到零时失败。

## RUN 页面按钮

- `Back to Builder`：返回 Assets 页面并保留全部选择。
- `Restart Game`：仅重新开始当前小游戏，不清空模板或 Assets。

## 当前实现范围

三个小游戏均为原生 HTML、CSS 和 JavaScript 实现，使用原创几何图形，不嵌入第三方网站。它们包含课堂展示需要的完整核心循环，但没有音频、复杂关卡、联网排行榜或商业游戏素材。

## 运行方式

直接打开 `index.html`，或者在项目目录运行：

```powershell
py -m http.server 4173 --bind 127.0.0.1
```

然后访问 `http://127.0.0.1:4173/`。
